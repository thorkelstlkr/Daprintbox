/*
 * Configuración de la app.
 * apiUrl: '' → los datos se guardan solo en este navegador.
 * apiUrl: 'api/api.php' → los datos se guardan en tu servidor (MySQL) y se comparten.
 */
window.DAPRINTBOX_CONFIG = {
  apiUrl: '',
};
